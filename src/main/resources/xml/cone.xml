<form name="КОНУС">
    <area>PI * radius * (radius + sin(alfa) * sqrt(pow(length, 2) + pow(radius, 2)))</area>
    <midsectionArea>PI * pow(radius, 2)</midsectionArea>
    <centrePressureX>cubesatSize.length + length / 3</centrePressureX>
    <centrePressureY>cubesatSize.width / 2</centrePressureY>
    <centrePressureZ>cubesatSize.height / 2</centrePressureZ>
    <centreMassX>cubesatSize.length + length / 4</centreMassX>
    <centreMassY>cubesatSize.width / 2</centreMassY>
    <centreMassZ>cubesatSize.height / 2</centreMassZ>
    <shellNtu>PI * radius * sqrt(pow(radius, 2) + pow(length, 2)) + PI * pow(radius, 2)</shellNtu>
</form>
