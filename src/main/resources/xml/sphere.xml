<form name="ШАР">
    <area>radius * radius * PI</area>
    <midsectionArea>radius * radius * PI</midsectionArea>
    <centrePressureX>cubesatSize.length + radius</centrePressureX>
    <centrePressureY>cubesatSize.width / 2</centrePressureY>
    <centrePressureZ>cubesatSize.height / 2</centrePressureZ>
    <centreMassX>cubesatSize.length + radius</centreMassX>
    <centreMassY>cubesatSize.width / 2</centreMassY>
    <centreMassZ>cubesatSize.height / 2</centreMassZ>
    <shellNtu>4 * PI * radius * radius</shellNtu>
</form>